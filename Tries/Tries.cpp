#include <iostream>
using namespace std;

int main() {
    

    return 0;
}

/*
trie si a generic tree that used for retrieval queries
used generally for insert & search operations
Searches in O(L). L = key length but uses extra space
*/