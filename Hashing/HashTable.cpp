#include <iostream>
using namespace std;

int main() {
    // Hash Table ia a unordered data structure (key, value)
    // Insert O(1)
    // Search O(1)
    // Remove O(1)

    // hash function -> index = hash(key) % size
    // hash function -> index = (key % size) + 1

    return 0;
}